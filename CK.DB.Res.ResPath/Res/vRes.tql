-- SetupConfig:{}
create transformer on CK.vRes
as
begin

	inject "
		left outer join CK.tResPath n on n.ResId = r.ResId" after single {from CK.tRes r};

	add column "ResPath = IsNull( n.ResPath, 'Auto/' + cast( r.ResId as varchar) )";

end
